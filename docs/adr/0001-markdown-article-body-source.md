# Markdown Article Body Source

The blog stores article body content as Markdown source text and derives previews and public reading pages from that source. Markdown was chosen over saving rendered HTML or a custom paragraph array because articles now need durable support for headings, tables, code blocks, and math formulas while remaining easy to edit in the Post Composer; raw HTML is not enabled, and legacy paragraph arrays are treated as Markdown-compatible plain text during normalization.
